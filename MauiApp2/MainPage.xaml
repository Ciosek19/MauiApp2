<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MauiApp2.MainPage">

    <ScrollView>
        <VerticalStackLayout
            Padding="30,0"
            Spacing="25">
            <Image
                Source="dotnet_bot.png"
                HeightRequest="185"
                Aspect="AspectFit"
                SemanticProperties.Description="dot net bot in a race car number eight" />

            <Label
                Text="Hello, World!"
                Style="{StaticResource Headline}"
                SemanticProperties.HeadingLevel="Level1" />

            <Label
                Text="Welcome to &#10;.NET Multi-platform App UI"
                Style="{StaticResource SubHeadline}"
                SemanticProperties.HeadingLevel="Level2"
                SemanticProperties.Description="Welcome to dot net Multi platform App U I" />

            <Button
                x:Name="CounterBtn"
                Text="Click me" 
                SemanticProperties.Hint="Counts the number of times you click"
                Clicked="OnCounterClicked"
                HorizontalOptions="Fill" />
            <Button
                x:Name="FotoBtn"
                Text="Foto pal feibu" 
                SemanticProperties.Hint="Foto pal feibu"
                Clicked="OnFotoBtnClicked"
                HorizontalOptions="Fill" />
            <Button
                x:Name="VideoBtn"
                Text="Videito pal insta" 
                SemanticProperties.Hint="Videito pal insta"
                Clicked="OnVideoBtnClicked"
                HorizontalOptions="Fill" />
            <Button
                x:Name="HuellaBtn"
                Text="Autenticate rastrillo" 
                SemanticProperties.Hint="Autenticate"
                Clicked="OnHuellaBtnClicked"
                HorizontalOptions="Fill" />
            <Button
                x:Name="UbicacionBtn"
                Text="Te tiro la ubi" 
                SemanticProperties.Hint="Autenticate"
                Clicked="OnUbicacionBtnClicked"
                HorizontalOptions="Fill" />
        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
